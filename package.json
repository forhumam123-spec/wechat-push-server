{
  "name": "wechat-push-server",
  "version": "1.0.0",
  "description": "FCM push notification server for we chat",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
